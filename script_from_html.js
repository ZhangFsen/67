<!doctype html>
<html lang="zh-CN">
<head>
<meta charset="utf-8">
<meta name="viewport" content="width=device-width,initial-scale=1,maximum-scale=1,user-scalable=no,viewport-fit=cover">
<title>云端用户管理器 Pro</title>
<style>
:root{--bg:#f5f7fb;--card:#fff;--text:#162033;--muted:#6b7280;--line:#e5eaf3;--blue:#1677ff;--blue2:#0f62d6;--green:#16a765;--orange:#f59e0b;--red:#ef4444;--purple:#7c3aed;--shadow:0 10px 28px rgba(22,32,51,.08)}
*{box-sizing:border-box;-webkit-tap-highlight-color:transparent}body{margin:0;background:var(--bg);font-family:-apple-system,BlinkMacSystemFont,"Segoe UI","Microsoft YaHei",Arial,sans-serif;color:var(--text)}button,input,select,textarea{font:inherit}button{border:0;cursor:pointer}input,textarea{outline:none}.page{min-height:100vh;padding:env(safe-area-inset-top) 0 calc(78px + env(safe-area-inset-bottom))}.top{height:58px;background:linear-gradient(135deg,#1478ff,#095bd8);color:#fff;display:flex;align-items:center;justify-content:space-between;padding:0 16px;position:sticky;top:0;z-index:5;box-shadow:0 4px 12px rgba(9,91,216,.18)}.top b{font-size:17px}.iconbtn{width:36px;height:36px;border-radius:12px;background:rgba(255,255,255,.14);color:#fff;font-size:18px}.wrap{padding:14px}.projectPill{background:#fff;color:#1660d6;border-radius:16px;padding:7px 10px;font-size:12px;max-width:128px;overflow:hidden;text-overflow:ellipsis;white-space:nowrap}.loginHero{min-height:100vh;background:linear-gradient(160deg,#0f72ff 0%,#0b5ed8 48%,#f5f7fb 48.1%);padding:58px 22px 28px;color:#fff}.brand{padding-top:28px}.brand h1{font-size:25px;margin:0 0 10px}.brand p{margin:0;color:rgba(255,255,255,.78)}.shield{width:116px;height:116px;margin:34px auto 48px;border-radius:36px;background:rgba(255,255,255,.18);display:grid;place-items:center;font-size:54px}.loginCard{background:#fff;color:var(--text);border-radius:22px;padding:22px;box-shadow:var(--shadow)}.field{margin:12px 0}.label{font-size:13px;color:var(--muted);margin:0 0 7px 2px}.inp,.sel,textarea{width:100%;border:1px solid var(--line);background:#f8fafc;border-radius:14px;padding:13px 14px;color:var(--text)}textarea{min-height:88px;resize:none}.primary{width:100%;background:linear-gradient(135deg,var(--blue),var(--blue2));color:#fff;border-radius:14px;padding:14px;font-weight:800;box-shadow:0 8px 18px rgba(22,119,255,.2)}.ghost{background:#eef2f7;color:var(--text);border-radius:14px;padding:12px 14px;font-weight:700}.dangerBtn{background:#feecec;color:#dc2626}.greenBtn{background:#e9f9f0;color:#119154}.mini{padding:8px 10px;border-radius:11px;font-size:12px}.card{background:var(--card);border:1px solid var(--line);border-radius:18px;padding:14px;margin:0 0 12px;box-shadow:var(--shadow)}.grid{display:grid;grid-template-columns:1fr 1fr;gap:10px}.stat{min-height:98px}.stat .name{color:var(--muted);font-size:13px}.stat .num{font-size:24px;color:#1155c8;font-weight:900;margin-top:8px}.stat .sub{font-size:12px;color:var(--green);margin-top:4px}.sectionTitle{font-weight:900;margin:18px 2px 10px}.toolbar{display:block;margin-bottom:10px}.toolbar .inp{display:block}.controlRow{display:grid;grid-template-columns:1fr 1fr;gap:10px;margin:10px 0}.filterLine{display:flex;align-items:center;justify-content:space-between;margin:6px 2px 12px;color:var(--muted);font-size:12px}.chipGrid{display:grid;grid-template-columns:1fr 1fr;gap:10px}.chipBig{border-radius:14px;background:#f8fafc;border:1px solid var(--line);padding:13px 12px;text-align:left;font-weight:700;color:var(--text)}.chipBig.on{background:#eaf2ff;color:#1463d8;border-color:#cfe3ff}.user{display:flex;gap:12px;align-items:flex-start}.avatar{width:44px;height:44px;border-radius:50%;background:#dbeafe;display:grid;place-items:center;color:#1677ff;font-weight:900;flex:0 0 44px}.userInfo{flex:1;min-width:0}.account{font-weight:900;font-size:15px}.uid{font-size:12px;color:var(--muted);margin-top:3px;overflow:hidden;text-overflow:ellipsis;white-space:nowrap}.meta{display:flex;flex-wrap:wrap;gap:8px;margin-top:8px;font-size:12px;color:var(--muted)}.tag{border-radius:999px;padding:4px 8px;font-size:12px;font-weight:800}.tag.pro{background:#e9f9f0;color:#16a765}.tag.perm{background:#f2ecff;color:#7c3aed}.tag.trial{background:#fff4df;color:#d97706}.tag.expired{background:#feecec;color:#dc2626}.tag.free{background:#f4f6fa;color:#64748b}.bottom{position:fixed;bottom:0;left:0;right:0;height:68px;background:rgba(255,255,255,.95);border-top:1px solid var(--line);display:flex;backdrop-filter:blur(10px);z-index:8;padding-bottom:env(safe-area-inset-bottom)}.tab{flex:1;display:flex;flex-direction:column;align-items:center;justify-content:center;color:#7b8494;font-size:11px;gap:3px}.tab span{font-size:21px}.tab.on{color:#1677ff;font-weight:900}.detailHead{display:flex;gap:14px;align-items:center}.bigAvatar{width:66px;height:66px;border-radius:50%;background:#dbeafe;display:grid;place-items:center;font-size:28px;color:#1677ff}.kv{display:flex;justify-content:space-between;gap:14px;border-bottom:1px solid #f0f3f8;padding:12px 0;font-size:14px}.kv:last-child{border-bottom:0}.kv span:first-child{color:var(--muted)}.kv span:last-child{text-align:right;word-break:break-all}.actions{display:grid;grid-template-columns:1fr 1fr 1fr;gap:12px}.act{background:#f8fafc;border:1px solid var(--line);border-radius:16px;min-height:82px;display:flex;flex-direction:column;align-items:center;justify-content:center;gap:8px;color:var(--text);font-size:12px}.act i{font-style:normal;font-size:24px}.project{display:flex;justify-content:space-between;gap:12px;align-items:center}.muted{color:var(--muted)}.small{font-size:12px}.toast{position:fixed;left:18px;right:18px;bottom:84px;background:rgba(22,32,51,.92);color:#fff;border-radius:14px;padding:12px 14px;text-align:center;z-index:40;display:none}.modal{position:fixed;inset:0;background:rgba(15,23,42,.42);z-index:30;display:none;align-items:flex-end}.sheet{background:#fff;border-radius:22px 22px 0 0;padding:18px;width:100%;max-height:88vh;overflow:auto;box-shadow:0 -10px 28px rgba(0,0,0,.12)}.sheet h3{margin:0 0 6px;font-size:18px}.sheet p{margin:8px 0 14px;color:var(--muted);line-height:1.5}.rowBtns{display:flex;gap:10px;margin-top:16px}.rowBtns button{flex:1}.modalIcon{width:42px;height:42px;border-radius:50%;display:grid;place-items:center;margin-bottom:10px;font-size:22px}.modalIcon.ok{background:#e9f9f0;color:#16a765}.modalIcon.warn{background:#fff4df;color:#d97706}.modalIcon.err{background:#feecec;color:#ef4444}.emptyCard{text-align:center;color:var(--muted);padding:28px 14px}.emptyIcon{width:56px;height:56px;border-radius:18px;background:#eef2f7;display:grid;place-items:center;margin:0 auto 12px;font-size:28px}.hidden{display:none!important}@media(min-width:600px){body{max-width:420px;margin:0 auto;background:#edf2f7}.bottom{left:calc(50% - 210px);right:calc(50% - 210px)}.modal{left:calc(50% - 210px);right:calc(50% - 210px)}}
</style>
</head>
<body>
<div id="app"></div>
<div class="toast" id="toast"></div>
<div class="modal" id="modal"><div class="sheet" id="sheet"></div></div>
<script>
const DEFAULT_PROJECT={name:'Colox 色库',base_url:'https://filemanagerauth-d5fmy6cv843c4a56-1419081389.ap-shanghai.app.tcloudbase.com',admin_token:'CHANGE_ME_TO_A_LONG_RANDOM_ADMIN_TOKEN',users_path:'/cox/admin/users',action_path:'/cox/admin/action'};
const LS='colox_admin_mobile_v2';
let state={authed:false,tab:'dash',users:[],filtered:[],query:'',filter:'全部',selected:null,config:null,loading:false};
let currentModal=null;
function load(){let c={password:'',current_project:'Colox 色库',projects:[DEFAULT_PROJECT]};try{let old=localStorage.getItem(LS)||localStorage.getItem('colox_admin_mobile_v1')||'';if(old)c=Object.assign(c,JSON.parse(old));}catch(e){}if(!Array.isArray(c.projects)||!c.projects.length)c.projects=[DEFAULT_PROJECT];if(!c.current_project)c.current_project=c.projects[0].name;state.config=c;}
function save(){localStorage.setItem(LS,JSON.stringify(state.config));}
function project(){let ps=state.config.projects||[];return ps.find(p=>p.name===state.config.current_project)||ps[0]||null;}
function esc(s){return String(s??'').replace(/[&<>"']/g,m=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[m]));}
function toast(t){let el=document.getElementById('toast');el.textContent=t;el.style.display='block';setTimeout(()=>el.style.display='none',2200);try{AndroidBridge.vibrate(45)}catch(e){}}
function fmt(v){let s=String(v||'').trim();if(!s)return '-';if(s==='永久')return '永久';return s.replace('T',' ').replace('Z','').split('.')[0].slice(0,16);}
function days(v){let s=String(v||'').trim();if(!s)return '-';if(s==='永久')return '永久';let d=new Date(s.replace(' ','T'));if(isNaN(d))return '-';let now=new Date();let n=Math.ceil((new Date(d.getFullYear(),d.getMonth(),d.getDate())-new Date(now.getFullYear(),now.getMonth(),now.getDate()))/86400000);return n<=0?'0':String(n);}
function level(u){let m=String(u.member_status||'');let p=String(u.member_permanent||'');if(u.status==='disabled'||u.status==='已禁用')return ['已禁用','expired'];if(days(u.member_expire_at)==='0')return ['已过期','expired'];if(m.includes('永久')||p==='1'||u.member_expire_at==='永久')return ['永久版','perm'];if(m.includes('专业')||u.member_plan==='pro')return ['专业版','pro'];if(m.includes('试用')||u.trial_status)return ['试用中','trial'];return ['免费版','free'];}
function normalizeUsers(data){if(Array.isArray(data))return data;let arr=data.users||data.data||data.list||data.items||[];return Array.isArray(arr)?arr:[];}
function apiUrl(kind){let p=project();if(!p)throw new Error('请先添加项目');let base=String(p.base_url||'').replace(/\/$/,'');let path=p[kind==='users'?'users_path':'action_path']||'';if(!path.startsWith('/'))path='/'+path;return base+path;}
function post(url,payload){let body=JSON.stringify(payload||{});if(window.AndroidBridge&&AndroidBridge.postJsonSync){let text=AndroidBridge.postJsonSync(url,body);try{return JSON.parse(text)}catch(e){return {ok:false,error:'云端返回不是 JSON：'+String(text).slice(0,220)}}}return fetch(url,{method:'POST',headers:{'Content-Type':'application/json'},body}).then(r=>r.json());}
function apiMessage(data,def){return (data&&(data.message||data.error||data.msg))||def;}
function showAlert(title,message,type='ok',onOk){currentModal={type:'alert',onOk};let cls=type==='error'?'err':(type==='warn'?'warn':'ok');let icon=type==='error'?'×':(type==='warn'?'!':'✓');document.getElementById('sheet').innerHTML=`<div class="modalIcon ${cls}">${icon}</div><h3>${esc(title)}</h3><p>${esc(message)}</p><div class="rowBtns"><button class="primary" onclick="modalOk()">确定</button></div>`;openModal();}
function showConfirm(title,message,onConfirm,danger=false){currentModal={type:'confirm',onConfirm};document.getElementById('sheet').innerHTML=`<div class="modalIcon ${danger?'err':'warn'}">${danger?'!':'?'}</div><h3>${esc(title)}</h3><p>${esc(message)}</p><div class="rowBtns"><button class="ghost" onclick="closeModal()">取消</button><button class="${danger?'ghost dangerBtn':'primary'}" onclick="modalConfirm()">确定</button></div>`;openModal();}
function showForm(title,subtitle,fields,onSubmit,confirmText='保存'){currentModal={type:'form',fields,onSubmit};let html=`<h3>${esc(title)}</h3>${subtitle?`<p>${esc(subtitle)}</p>`:''}`;fields.forEach(f=>{html+=`<div class="field"><div class="label">${esc(f.label||f.name)}${f.required?' *':''}</div>`;let val=esc(f.value||'');if(f.textarea)html+=`<textarea id="mf_${esc(f.name)}" placeholder="${esc(f.placeholder||'')}">${val}</textarea>`;else html+=`<input class="inp" id="mf_${esc(f.name)}" type="${esc(f.type||'text')}" value="${val}" placeholder="${esc(f.placeholder||'')}">`;html+=`</div>`});html+=`<div class="rowBtns"><button class="ghost" onclick="closeModal()">取消</button><button class="primary" onclick="modalSubmit()">${esc(confirmText)}</button></div>`;document.getElementById('sheet').innerHTML=html;openModal();setTimeout(()=>{let first=document.querySelector('#sheet input,#sheet textarea');if(first)first.focus();},80);}
function openModal(){document.getElementById('modal').style.display='flex';}
function closeModal(){document.getElementById('modal').style.display='none';currentModal=null;}
function modalOk(){let cb=currentModal&&currentModal.onOk;closeModal();if(cb)cb();}
function modalConfirm(){let cb=currentModal&&currentModal.onConfirm;closeModal();if(cb)cb();}
function modalSubmit(){if(!currentModal)return;let values={};for(let f of currentModal.fields){let el=document.getElementById('mf_'+f.name);let v=(el?el.value:'').trim();if(f.required&&!v){toast('请填写：'+(f.label||f.name));if(el)el.focus();return;}values[f.name]=v;}let cb=currentModal.onSubmit;closeModal();if(cb)cb(values);}
document.getElementById('modal').onclick=e=>{if(e.target.id==='modal')closeModal();};
function loginHtml(){return `<div class="loginHero"><div class="brand"><h1>云端用户管理器 Pro</h1><p>安全 · 高效 · 便捷</p></div><div class="shield">🛡️</div><div class="loginCard"><h3 style="text-align:center;margin:0 0 18px">管理员登录</h3><input id="pwd" class="inp" type="password" placeholder="请输入管理员密码"><label class="small muted" style="display:flex;gap:8px;align-items:center;margin:12px 0"><input type="checkbox" id="remember"> 记住登录状态</label><button class="primary" onclick="login()">登录</button><div class="small muted" style="text-align:center;margin-top:28px">版本 v1.0.2</div></div></div>`;}
function login(){let pwd=document.getElementById('pwd').value;if(!state.config.password){if(!pwd||pwd.length<6){showAlert('密码太短','首次使用，请输入至少 6 位后台管理密码。','warn');return;}state.config.password=pwd;save();state.authed=true;toast('已设置管理密码');render();return;}if(pwd!==state.config.password){showAlert('登录失败','管理员密码错误。','error');return;}state.authed=true;render();}
function shell(title,body,right=''){let p=project();return `<div class="page"><div class="top"><button class="iconbtn" onclick="openProjectSwitch()">☰</button><b>${esc(title)}</b>${right||`<button class="projectPill" onclick="openProjectSwitch()">${esc(p?.name||'无项目')}</button>`}</div><div class="wrap">${body}</div></div>${tabs()}`;}
function tabs(){return `<div class="bottom"><div class="tab ${state.tab==='dash'?'on':''}" onclick="go('dash')"><span>⌂</span>仪表盘</div><div class="tab ${state.tab==='users'?'on':''}" onclick="go('users')"><span>👥</span>用户管理</div><div class="tab ${state.tab==='projects'?'on':''}" onclick="go('projects')"><span>▣</span>项目管理</div><div class="tab ${state.tab==='me'?'on':''}" onclick="go('me')"><span>⚙</span>我的</div></div>`;}
function go(t){state.tab=t;render();if(t==='users'&&!state.users.length)refresh();}
function stats(){let total=state.users.length,pro=0,perm=0,online=0,free=0,disabled=0;state.users.forEach(u=>{let l=level(u)[0];if(l==='永久版')perm++;else if(l==='专业版')pro++;else if(l==='免费版')free++;if(u.status==='disabled'||u.status==='已禁用')disabled++;if(String(u.current_login_device||'').trim())online++;});return {total,pro,perm,online,free,disabled};}
function dashHtml(){let s=stats();let recent=state.users.slice(0,3).map(u=>`<div class="kv"><span>${esc(u.account||u.uid)}</span><span>${esc(level(u)[0])}</span></div>`).join('')||'<div class="emptyCard"><div class="emptyIcon">👥</div><b>暂无数据</b><br><span class="small">点击刷新用户数据</span></div>';return shell('仪表盘',`<div class="grid"><div class="card stat"><div class="name">用户总数</div><div class="num">${s.total}</div><div class="sub">当前项目</div></div><div class="card stat"><div class="name">专业版</div><div class="num">${s.pro}</div><div class="sub">含非永久</div></div><div class="card stat"><div class="name">永久版</div><div class="num">${s.perm}</div><div class="sub">永久专业</div></div><div class="card stat"><div class="name">在线设备</div><div class="num">${s.online}</div><div class="sub">当前登录</div></div></div><button class="primary" onclick="refresh()">${state.loading?'正在刷新...':'刷新用户数据'}</button><div class="sectionTitle">最近用户</div><div class="card">${recent}</div>`);}
async function refresh(){let p=project();if(!p){showAlert('暂无项目','请先添加项目配置。','warn');return;}state.loading=true;render();try{let data=await Promise.resolve(post(apiUrl('users'),{admin_token:p.admin_token}));if(data&&data.ok===false)throw new Error(apiMessage(data,'读取用户失败'));state.users=normalizeUsers(data);state.selected=state.selected?state.users.find(u=>u.uid===state.selected.uid)||null:null;applyFilter(false);toast('用户数据已刷新');}catch(e){showAlert('刷新失败',String(e.message||e),'error');}finally{state.loading=false;render();}}
function applyFilter(doRender=true){let q=(state.query||'').toLowerCase().trim();state.filtered=state.users.filter(u=>{let hay=[u.account,u.uid,u.remark,u.current_login_device,u.trial_machine_code].join(' ').toLowerCase();if(q&&!hay.includes(q))return false;let l=level(u)[0];if(state.filter==='全部')return true;if(state.filter==='在线设备')return String(u.current_login_device||'').trim();if(state.filter==='已过期')return l==='已过期';return l===state.filter;});if(doRender)render();}
function usersHtml(){if(!state.filtered.length&&state.users.length)applyFilter(false);let cards=(state.filtered||[]).map((u,i)=>userCard(u,i)).join('')||'<div class="card emptyCard"><div class="emptyIcon">👥</div><b>暂无用户数据</b><br><span class="small">请检查项目配置或点击刷新</span><button class="primary" style="margin-top:14px" onclick="refresh()">刷新数据</button></div>';return shell('用户管理',`<div class="toolbar"><input class="inp" placeholder="搜索手机号 / UID / 备注" value="${esc(state.query)}" oninput="state.query=this.value;applyFilter()"></div><div class="controlRow"><button class="ghost" onclick="refresh()">刷新数据</button><button class="ghost" onclick="openFilterSheet()">筛选：${esc(state.filter)}</button></div><div class="filterLine"><span>显示 ${state.filtered.length} / ${state.users.length} 个用户</span><span>${state.filter==='全部'?'全部状态':esc(state.filter)}</span></div>${cards}`,`<button class="iconbtn" onclick="createUser()">＋</button>`);}
function userCard(u,i){let [txt,cls]=level(u);return `<div class="card user" onclick="openUser('${esc(u.uid||i)}')"><div class="avatar">${esc(String(u.account||'?').slice(0,1))}</div><div class="userInfo"><div style="display:flex;justify-content:space-between;gap:8px"><div class="account">${esc(u.account||'未命名账号')}</div><span class="tag ${cls}">${txt}</span></div><div class="uid">UID: ${esc(u.uid||'-')}</div><div class="meta"><span>${u.status==='disabled'||u.status==='已禁用'?'已禁用':'正常'}</span><span>到期：${fmt(u.member_expire_at)}</span><span>剩余：${days(u.member_expire_at)}</span></div></div></div>`;}
function openFilterSheet(){let opts=['全部','专业版','永久版','免费版','已禁用','已过期','在线设备'];document.getElementById('sheet').innerHTML=`<h3>筛选用户</h3><p>选择要查看的用户状态。</p><div class="chipGrid">${opts.map(x=>`<button class="chipBig ${state.filter===x?'on':''}" onclick="state.filter='${x}';closeModal();applyFilter()">${x}</button>`).join('')}</div><button class="ghost" style="width:100%;margin-top:14px" onclick="closeModal()">取消</button>`;openModal();}
function findUser(id){return state.users.find((u,i)=>String(u.uid||i)===String(id));}
function openUser(id){state.selected=findUser(id);state.tab='detail';render();}
function detailHtml(){let u=state.selected;if(!u)return usersHtml();let [txt,cls]=level(u);return shell('用户详情',`<div class="card detailHead"><div class="bigAvatar">👤</div><div><h3 style="margin:0 0 6px">${esc(u.account||'-')}</h3><div class="small muted">UID: ${esc(u.uid||'-')}</div><div style="margin-top:8px"><span class="tag ${cls}">${txt}</span></div></div></div><div class="card"><div class="kv"><span>账号状态</span><span>${esc(u.status||'正常')}</span></div><div class="kv"><span>到期时间</span><span>${fmt(u.member_expire_at)}</span></div><div class="kv"><span>剩余天数</span><span>${days(u.member_expire_at)}</span></div><div class="kv"><span>注册时间</span><span>${fmt(u.registered_at)}</span></div><div class="kv"><span>最后登录</span><span>${fmt(u.last_login_at)}</span></div><div class="kv"><span>登录次数</span><span>${esc(u.login_count||'0')}</span></div><div class="kv"><span>当前设备</span><span>${esc(u.current_login_device||'-')}</span></div><div class="kv"><span>机器码</span><span>${esc(u.trial_machine_code||'-')}</span></div><div class="kv"><span>备注</span><span>${esc(u.remark||'-')}</span></div></div><button class="primary" onclick="state.tab='ops';render()">管理操作</button><button class="ghost" style="width:100%;margin-top:10px" onclick="go('users')">返回列表</button>`);}
function opsHtml(){let u=state.selected;if(!u)return usersHtml();let uid=u.uid||'';let items=[['♛','设专业版',`setPro('${esc(uid)}')`],['💎','永久版',`confirmAct('set_permanent','${esc(uid)}','确定将该用户设为永久专业版？')`],['📅','改到期',`updateExpire('${esc(uid)}')`],['🆓','免费版',`confirmAct('set_free','${esc(uid)}','确定将该用户设为免费版？')`],['🔑','改密码',`updatePassword('${esc(uid)}')`],['🚪','踢下线',`confirmAct('kick_session','${esc(uid)}','确定踢下线该用户？')`],['⛔','禁用',`confirmAct('disable_user','${esc(uid)}','确定禁用该账号？',true)`],['✅','启用',`confirmAct('enable_user','${esc(uid)}','确定启用该账号？')`],['🧹','清机器',`confirmAct('clear_machine_binding','${esc(uid)}','确定清除机器绑定？')`],['🔄','重置试用',`confirmAct('reset_trial','${esc(uid)}','确定重置试用状态？')`],['📝','备注',`updateRemark('${esc(uid)}')`],['🗑️','删除',`deleteUser('${esc(uid)}')`]];return shell('用户操作',`<div class="card detailHead"><div class="bigAvatar">👤</div><div><h3 style="margin:0 0 6px">${esc(u.account||'-')}</h3><div class="small muted">${esc(uid||'-')}</div></div></div><div class="actions">${items.map(a=>`<button class="act" onclick="${a[2]}"><i>${a[0]}</i>${a[1]}</button>`).join('')}</div><button class="ghost" style="width:100%;margin-top:14px" onclick="state.tab='detail';render()">返回详情</button>`);}
async function runAction(action,uid,extra){let p=project();if(!p){showAlert('暂无项目','请先添加项目配置。','warn');return;}try{let payload=Object.assign({admin_token:p.admin_token,action,uid},extra||{});let data=await Promise.resolve(post(apiUrl('action'),payload));if(data&&data.ok===false)throw new Error(apiMessage(data,'操作失败'));showAlert('操作成功',apiMessage(data,'操作成功。'),'ok',()=>refresh());}catch(e){showAlert('操作失败',String(e.message||e),'error');}}
function setPro(uid){showForm('设为专业版','请输入会员天数。',[{name:'days',label:'会员天数',value:'30',type:'number',required:true}],v=>runAction('set_pro',uid,{days:parseInt(v.days)||30}),'确定');}
function updateExpire(uid){let u=state.selected||{};showForm('修改到期时间','格式示例：2026-06-30 23:59',[{name:'expire_at',label:'到期时间',value:u.member_expire_at==='永久'?'':(u.member_expire_at||''),placeholder:'2026-06-30 23:59',required:true}],v=>runAction('update_expire',uid,{expire_at:v.expire_at}),'保存');}
function updatePassword(uid){showForm('修改用户密码','请输入新的登录密码，至少 6 位。',[{name:'password',label:'新密码',type:'password',required:true}],v=>{if(v.password.length<6){showAlert('密码太短','用户密码至少 6 位。','warn');return;}runAction('update_password',uid,{password:v.password});},'保存');}
function updateRemark(uid){let u=state.selected||{};showForm('修改备注','备注会同步保存到云端。',[{name:'remark',label:'备注',value:u.remark||'',textarea:true,placeholder:'请输入备注'}],v=>runAction('update_remark',uid,{remark:v.remark}),'保存');}
function confirmAct(action,uid,msg,danger=false){showConfirm('确认操作',msg,()=>runAction(action,uid,{}),danger);}
function deleteUser(uid){showConfirm('删除用户','确定删除此用户吗？此操作不可恢复。',()=>runAction('delete_user',uid,{}),true);}
function createUser(){showForm('新增账号','后台创建账号后，用户即可登录软件。',[{name:'account',label:'账号（手机号 / 邮箱 / 昵称）',required:true},{name:'password',label:'密码',type:'password',value:'123456',required:true},{name:'password2',label:'确认密码',type:'password',value:'123456',required:true}],v=>{if(v.password.length<6){showAlert('密码太短','密码至少 6 位。','warn');return;}if(v.password!==v.password2){showAlert('确认失败','两次输入的密码不一致。','warn');return;}runAction('create_user','',{account:v.account,password:v.password});},'创建账号');}
function projectsHtml(){let ps=state.config.projects||[];let body=ps.map((p,i)=>`<div class="card project"><div style="min-width:0"><h3 style="margin:0 0 7px">${esc(p.name)}</h3><div class="small muted">${esc(p.base_url)}</div><div class="small muted">${esc(p.users_path)} ｜ ${esc(p.action_path)}</div><div class="small" style="margin-top:8px;color:${p.name===state.config.current_project?'#16a765':'#6b7280'}">${p.name===state.config.current_project?'当前项目':'点击编辑可设为当前项目'}</div></div><div><button class="ghost mini" onclick="editProject(${i})">编辑</button><button class="ghost mini dangerBtn" style="margin-top:6px" onclick="delProject(${i})">删除</button></div></div>`).join('')||'<div class="card emptyCard"><div class="emptyIcon">▣</div><b>暂无项目</b></div>';return shell('项目管理',`${body}<button class="primary" onclick="editProject(-1)">添加新项目</button>`);}
function editProject(i){let ps=state.config.projects||[];let p=i>=0?ps[i]:{name:'新项目',base_url:'https://',admin_token:'',users_path:'/cox/admin/users',action_path:'/cox/admin/action',remark:''};showForm(i>=0?'编辑项目':'添加项目','一次性填写全部项目配置，不再逐个弹窗。',[{name:'name',label:'软件名称',value:p.name||'',placeholder:'例如：Colox 色库',required:true},{name:'base_url',label:'后台地址',value:p.base_url||'',placeholder:'https://xxxx.tcloudbase.com',required:true},{name:'users_path',label:'读取用户接口',value:p.users_path||'/cox/admin/users',required:true},{name:'action_path',label:'执行操作接口',value:p.action_path||'/cox/admin/action',required:true},{name:'admin_token',label:'后台访问密钥 Admin Token',value:p.admin_token||'',placeholder:'请输入后台访问密钥',required:true},{name:'remark',label:'备注（可选）',value:p.remark||'',textarea:true,placeholder:'请输入备注'}],v=>{let np={name:v.name,base_url:v.base_url.replace(/\/$/,''),users_path:v.users_path.startsWith('/')?v.users_path:'/'+v.users_path,action_path:v.action_path.startsWith('/')?v.action_path:'/'+v.action_path,admin_token:v.admin_token,remark:v.remark};if(i>=0)ps[i]=np;else ps.push(np);state.config.projects=ps;state.config.current_project=np.name;save();state.users=[];state.filtered=[];toast('项目已保存');render();},'保存项目');}
function delProject(i){showConfirm('删除项目','确定删除此项目配置吗？不会删除云端用户数据。',()=>{let ps=state.config.projects||[];ps.splice(i,1);state.config.projects=ps;state.config.current_project=ps[0]?.name||'';save();state.users=[];state.filtered=[];render();},true);}
function openProjectSwitch(){let ps=state.config.projects||[];let html=`<h3>切换项目</h3><p>选择当前要管理的软件后台。</p>${ps.map((p,i)=>`<button class="ghost" style="width:100%;margin:6px 0;text-align:left" onclick="switchProject(${i})">${esc(p.name)}${p.name===state.config.current_project?'  ✓':''}</button>`).join('')}<button class="primary" style="margin-top:10px" onclick="closeModal();go('projects')">项目管理</button><button class="ghost" style="width:100%;margin-top:10px" onclick="closeModal()">取消</button>`;document.getElementById('sheet').innerHTML=html;openModal();}
function switchProject(i){let p=(state.config.projects||[])[i];if(!p)return;state.config.current_project=p.name;save();state.users=[];state.filtered=[];closeModal();state.tab='dash';render();refresh();}
function meHtml(){return shell('我的',`<div class="card"><h3>本机设置</h3><div class="kv"><span>当前项目</span><span>${esc(project()?.name||'无')}</span></div><div class="kv"><span>本机密码</span><span>${state.config.password?'已设置':'未设置'}</span></div></div><button class="ghost" style="width:100%;margin-bottom:10px" onclick="changePwd()">修改管理密码</button><button class="ghost dangerBtn" style="width:100%;margin-bottom:10px" onclick="logout()">锁定后台</button><button class="ghost dangerBtn" style="width:100%" onclick="resetAll()">清空本机配置</button><div class="card small muted" style="margin-top:16px">说明：此手机版基于 Android WebView 模板制作。项目配置保存在手机本机，云端接口仍使用原来的 users_path / action_path。</div>`);}
function changePwd(){showForm('修改管理密码','请输入新的本机后台密码。',[{name:'password',label:'新管理密码',type:'password',required:true},{name:'password2',label:'确认密码',type:'password',required:true}],v=>{if(v.password.length<6){showAlert('密码太短','管理密码至少 6 位。','warn');return;}if(v.password!==v.password2){showAlert('确认失败','两次输入的密码不一致。','warn');return;}state.config.password=v.password;save();showAlert('修改成功','管理密码已修改。','ok');},'保存');}
function logout(){state.authed=false;render();}
function resetAll(){showConfirm('清空配置','确定清空本机项目配置和管理密码吗？',()=>{localStorage.removeItem(LS);localStorage.removeItem('colox_admin_mobile_v1');load();state.authed=false;state.users=[];state.filtered=[];render();},true);}
function render(){let app=document.getElementById('app');if(!state.config)load();if(!state.authed){app.innerHTML=loginHtml();return;}if(state.tab==='dash')app.innerHTML=dashHtml();else if(state.tab==='users')app.innerHTML=usersHtml();else if(state.tab==='detail')app.innerHTML=detailHtml();else if(state.tab==='ops')app.innerHTML=opsHtml();else if(state.tab==='projects')app.innerHTML=projectsHtml();else app.innerHTML=meHtml();}
window.appBack=function(){if(document.getElementById('modal').style.display==='flex'){closeModal();return true;}if(state.tab==='ops'){state.tab='detail';render();return true;}if(state.tab==='detail'){state.tab='users';render();return true;}if(state.tab!=='dash'){state.tab='dash';render();return true;}return false;};
load();render();
</script>
</body>
</html>
